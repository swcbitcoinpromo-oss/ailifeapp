# Copy to .env and fill in. NEVER commit the real .env.
ANTHROPIC_API_KEY=
# Model used for all coach calls — adjust as newer models ship.
CLAUDE_MODEL=claude-sonnet-5

# Runway (briefing video generation, Phase 5) — fill in once subscribed.
RUNWAYML_API_SECRET=
# Verify the current video model name at dev.runwayml.com before first run.
RUNWAY_MODEL=gen4_turbo
